apiVersion: v1
kind: Service
metadata:
  name: $APPLICATION_NAME
  namespace: $APPLICATION_ENVIRONMENT
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8001
      name: http
  selector:
    app: $APPLICATION_NAME
    environment: $CI_ENVIRONMENT_SLUG